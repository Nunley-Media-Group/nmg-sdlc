# Spec Discovery (v3)

**Read when** write-spec for non-bug/spike needs to decide amend vs create.

For #N: glob specs/{N}-* 

If present and matches singular **Issue**: #N and open/undelivered: update that dir.

Else create specs/{N}-{slug-from-title}/ 

No epic routing, no umbrella, no amendment to other N's dir. Simple number match only.

Use ask only if slug collision needs user choice (rare, 1 of budget). 
