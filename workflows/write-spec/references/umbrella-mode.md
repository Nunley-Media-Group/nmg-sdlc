# Umbrella (deleted in v3)

Epic coordination, child packages, aggregate, epic-scope, first/later child modes removed.

All specs are executable {N}-slug/ with singular **Issue**. No epic ever owns a spec.
