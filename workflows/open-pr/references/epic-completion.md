# Post-Merge Epic Completion (removed in v3)

Epic ancestor reconciliation and coordination-only epics are deleted in v3.

open-pr succeeds only after exact PR MERGED and issue CLOSED for the executable #N. No leaf-to-root close of coordination containers. No calls to classifyEpicCompletion or related.

If legacy epic content is encountered during upgrade only.
