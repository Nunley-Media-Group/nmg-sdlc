- **P2 — The ordering test does not verify identical bar evidence** (`tests/test_market_data.py:336-359`). It checks each symbol’s bar count and final timestamp, then compares confirmation reasons. Different, unsorted, or duplicate bars could satisfy those assertions if the reason stays the same. Compare each symbol’s complete bar sequence across orderings and assert timestamps are sorted and unique; the BDD step at `tests/features/steps/complete_iex_setup_evidence_steps.py:54-55` currently claims those properties without checking them.
