- **Post-session bars can enter execution evidence** (`src/pennyscan/market_data.py:145-155,425-446`). The final window ends at the regular-session close, but validation accepts a bar stamped exactly at that endpoint. If evaluation occurs at least one minute after close, `acquire_execution` includes that bar as completed regular-session evidence. Reject bars whose timestamp is at or after `open_at + REGULAR_SESSION_DURATION`.

- **tests/test_market_data.py:359–365** — The ordering test compares only `[0]` from `confirm_setup_trigger` and never asserts that confirmation succeeds. If both orderings reject a symbol, the test can pass even when their confirmation reasons differ; it does not establish the feature step’s “same confirmation reason” claim. Assert both the confirmation result and its reason for each symbol.
- **tests/test_market_data.py:301,347,380,395** — `submit_order_calls` is initialized to zero but no fake broker is connected to the exercised path. The zero-count assertions cannot detect an order submission, so the “neither batch submits a broker order” acceptance claim is not verified.
