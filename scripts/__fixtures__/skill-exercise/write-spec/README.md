# write-spec Exercise Fixture

`artifacts/write-spec-pass.json` is a complete executable four-file package. `artifacts/forbidden-obligations-fail.json` retains external burdens and must fail with artifact-scoped diagnostics. The rubric is `../rubrics/write-spec.md`.
