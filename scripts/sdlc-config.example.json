{
  "projectPath": "/path/to/your/project",
  "pluginRoot": "/Users/you/.claude/plugins/cache/nmg-plugins/nmg-sdlc/<version>",
  "pluginsPath": "/path/to/nmg-plugins",
  "_plugin_root_note": "Set pluginRoot for Claude Code plugin-cache installs (points directly at the plugin root with skills/ as a direct child). Set pluginsPath for the legacy nmg-plugins monorepo layout (runner appends plugins/nmg-sdlc). At least one is required; if both are set, pluginRoot wins.",
  "model": "sonnet",
  "logDir": "",
  "maxLogDiskUsageMB": 500,
  "_cleanup_note": "processPatterns is a Phase 2 fallback; Phase 1 uses the Claude subprocess PID tree",
  "_draft_issue_note": "draft-issue is interactive-only as of plugin v1.41.0 (issue #116); do not add a draftIssue step",
  "cleanup": {
    "processPatterns": ["--remote-debugging-port"]
  },
  "steps": {
    "startCycle":   { "model": "haiku",                      "maxTurns": 10,  "timeoutMin": 5  },
    "startIssue":   { "model": "sonnet", "effort": "low",    "maxTurns": 25,  "timeoutMin": 5,  "skill": "start-issue" },
    "writeSpecs":   { "model": "opus",   "effort": "xhigh",  "maxTurns": 60,  "timeoutMin": 15, "skill": "write-spec" },
    "implement":    { "model": "opus",   "effort": "xhigh",  "maxTurns": 150, "timeoutMin": 30, "skill": "write-code" },
    "simplify":     { "model": "sonnet", "effort": "medium", "maxTurns": 60,  "timeoutMin": 15 },
    "verify":       { "model": "opus",   "effort": "high",   "maxTurns": 100, "timeoutMin": 20, "skill": "verify-code" },
    "commitPush":   { "model": "haiku",                      "maxTurns": 20,  "timeoutMin": 8,  "skill": "commit-push" },
    "createPR":     { "model": "sonnet", "effort": "low",    "maxTurns": 45,  "timeoutMin": 5,  "skill": "open-pr" },
    "monitorCI":    { "model": "sonnet", "effort": "medium", "maxTurns": 60,  "timeoutMin": 20 },
    "merge":        { "model": "haiku",                      "maxTurns": 10,  "timeoutMin": 5  }
  },
  "maxRetriesPerStep": 3,
  "maxBounceRetries": 3
}
